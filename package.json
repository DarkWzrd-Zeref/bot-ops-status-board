{
  "name": "area-67",
  "private": true,
  "version": "0.2.0",
  "description": "AREA 67 — Palworld-style agent base. Palbox radius, build ghosts, pal tracker.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-icons.mjs && vite --host 127.0.0.1 --port 4611",
    "build": "tsc && node scripts/gen-icons.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4611",
    "test": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "3.88.2"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vite": "7.1.5"
  }
}
